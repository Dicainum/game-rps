import readlineSync from 'readline-sync';

const startRPS = () => {
  console.log('Добро пожаловать в "Камень-Ножницы-Бумага!');
  const playerchoice = readlineSync.question('Выберите вашу фигуру: ');
};

  

export default startRPS;